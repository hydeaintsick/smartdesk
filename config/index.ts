import { config as _config } from "./_config";

export const config = {
  ..._config,
  // Add any additional configuration here
  // You can overload anything in here such as title, Jira config, etc.
  // For example, you can add a base URL for API requests
  // See documentation for more details
};
