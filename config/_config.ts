export const $_APP_NAME = "SmartDesk";
export const $_HEADLINE = "Revolutionize Your IT Support Experience";
export const $_SUBHEADLINE = `Effortless ticket submission and resolution with ${$_APP_NAME}.`;
export const $_TAG = $_APP_NAME[0]?.toUpperCase() || "X".toUpperCase();

export const config = {
  appName: $_APP_NAME,
  headline: $_HEADLINE,
  subHeadline: $_SUBHEADLINE,
  tag: $_TAG,
};

export default config;
